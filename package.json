{
  "name": "parkingapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"cd CORE && node server.js\" \"cd gui && npm run start\"",
    "core-dependencies": "npm install --prefix CORE",
    "gui-dependencies": "npm install --prefix gui",
    "install-all-deps": "concurrently \"npm run core-dependencies\" \"npm run gui-dependencies\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.4.0"
  }
}
