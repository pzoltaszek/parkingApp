{
  "name": "parkingapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"cd CORE && node server.js\" \"cd gui && npm run start\"",
    "core-dependencies": "cd CORE && npm install",
    "gui-dependencies": "cd gui && npm install",
    "install-all-deps": "concurrently \"npm run core-dependencies\" \"npm run gui-dependencies\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.4.0"
  }
}
